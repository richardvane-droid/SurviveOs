# SurviveOs

一套自建的乡野生存系统。本仓库是权威文档层。

## 编号规范

模块四位编号 = 区域两位 + 序号。

区域:

- 00 总览
- 01 暖村木屋
- 02 地堡
- 03 阁楼间
- 04 阁楼仓库
- 05 庭院
- 06 草地
- 07 钓台
- 08 虚拟空间(跨区域信息层 + 移动设备)
- 09 设想

**一个模块 = 一个文件夹 = 一个 MD**：在对应区域目录下建 `四位编号-模块名/` 文件夹,
里面固定放一份 `README.md` 承载这个模块的正文(GitHub 打开文件夹会直接渲染它,手机上点进去就能看)。
模块产生代码、STL、图片等资产时,放进同一个文件夹下的 `code/`、`stl/`、`images/` 子目录,
单页 HTML 方案文档也放这里,README.md 负责索引它们。

文件夹名必须和 Drive 素材库里那个模块文件夹的名字**逐字一致**(见 `00-总览/0009-仓库与Drive素材对照表.md`)。

两个例外:

- `00-总览/` 里是元文档不是模块,保持单个文件(`0001-总览与方法论.md` 这样),不套文件夹
- 几个模块共用同一份方案时,合用一个文件夹,文件夹名把编号范围写全
  (例如 `06-草地/0601-0603-稀树草原环境系统/`),但 Drive 那边仍是一个模块一个文件夹

## 方法论

每个模块按三阶段推进,文档也按这三段组织:

1. 方案原理推演
2. 设备电商平台选型
3. 施工

智能生态默认栈:华为智慧生活 + HomeAssistant + ESPHome。
选型时优先考虑这三者的兼容性,偏离默认栈要在文档里写明理由。

## 分层:什么在这里,什么在 Drive

本仓库只放纯文本文档:方案推演、选型结论、施工要点、模块依赖关系。
二进制素材全部在 Google Drive 的 `SurviveOs-素材/`,不要提交进 git。

素材包括:施工照片、设备说明书 PDF、商品详情页截图、手绘草图拍照、视频。

## 素材引用约定

每个模块文档底部固定一段:

```
## 素材

Drive: SurviveOs-素材/<区域目录>/<四位编号-模块名>/
- <日期> <说明>(施工照片/)
- <型号> 说明书(说明书与保修卡/)
```

## 版式基准

`00-总览/0008-户型图与空间标注.html` 是位置标注和版式的权威来源。
设想版(surviveos-vision)的位置、版式、铅笔画一律以它为准。

## 工作流

- `inbox/` — 手机上随手记的任务单,格式不限,处理完删除或归档
- `outputs/` — agent 产出的落地区,review 后并入正式模块文档

## 边界

- 不要提交密钥、证件、财务信息。发现了立刻停下来告诉我。
- 设想版仓库 surviveos-vision 和本仓库**都是公开的**,内容不要互相复制。
- 本仓库公开,所以只写能公开的内容:不写门牌号/精确地址、不写家人个人信息、不贴带家门与车牌的照片。拿不准的素材放 Drive(Drive 是私有的),仓库里只写文字结论。
- 设想版全部功能按一个人设计,不涉及家人和招待朋友。
