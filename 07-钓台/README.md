# 07 钓台

目前唯一还没有子模块清单的区域。见 `00-总览/0002-区域与子模块清单.md` 里的 0700 条目——第一次推进这个区域时，先做一次范围拆解（要不要防雨顶棚、储物、照明、电源等），拆解完成后把 0700 替换成正式的 0701、0702…… 模块列表，并按根目录 `CLAUDE.md` 的口径给每个新模块建 `四位编号-模块名/README.md`。范围拆解本身的记录写在 `0700-范围拆解/README.md` 里。
